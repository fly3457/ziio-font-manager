# Ziio Font Manager Beta 开发计划

## 状态

- 当前阶段：Beta / v0.3 planning。
- 最后更新：2026-07-03。
- 当前目标：在 MVP 能力已经跑通的基础上，补齐稳定性、权限体验、可观测性、发布工程和文档体系。

## 摘要

Ziio Font Manager 是一个 Windows 11 本地字体管理工具，技术栈为 Go、Wails v2.12、React、TypeScript、Vite 和 SQLite。当前版本已经具备字体库扫描、系统字库扫描、字体检索、收藏、文件夹树、懒加载预览、安装 / 卸载和基础操作反馈能力。

Beta 阶段不再以“补齐主流程”为核心，而是转向面向真实字体库和真实 Windows 环境的可靠性打磨：大目录扫描不能拖住应用，坏字体不能拖住扫描，权限失败必须可理解，批量操作必须可追踪，构建和发布流程必须可重复。

## 当前代码分析

### 技术栈与运行形态

- 桌面壳使用 Wails v2.12，入口在 `main.go`，窗口标题为 `Ziio Font Manager`，默认尺寸为 1360x860。
- 后端使用 Go 1.25，核心依赖包括 `modernc.org/sqlite`、`github.com/tdewolff/font`、`golang.org/x/sys` 和 Wails。
- 前端使用 React 18、TypeScript 4.6、Vite 3 和 `lucide-react`，主界面集中在 `frontend/src/App.tsx`。
- Wails 绑定服务包括 `LibraryService`、`FontService`、`InstallService` 和 `App.GetAppInfo()`。
- 预览字体通过 Wails AssetServer 的自定义 handler 暴露为 `/preview-fonts/*.otf`。

### 后端服务边界

- `internal/library` 是 Wails 暴露给前端的服务层：
  - `LibraryService` 负责选择 / 添加字体库、扫描系统字库、重新扫描、列出字体库和文件夹。
  - `FontService` 负责搜索字体、读取详情、收藏、生成预览和在资源管理器中定位文件。
  - `InstallService` 负责安装、快捷安装、卸载和安装进度事件。
- `internal/scanner` 负责目录遍历、过滤支持格式、增量跳过未变化文件、解析超时和扫描状态更新。
- `internal/fontmeta` 负责字体格式识别、现代 SFNT / WOFF / EOT 元数据解析、旧格式降级、预览子集生成。
- `internal/winfont` 负责 Windows 字体目录、注册表 value、GDI 字体加载 / 移除、管理员权限检测和系统字体保护。
- `internal/store` 是 SQLite 存储层，集中维护 schema、迁移、查询、扫描任务、收藏、安装记录和操作日志。

### SQLite schema

当前 schema 包含：

- `library_roots`：字体库根目录，带 `kind=user/system`、启用状态和最近扫描时间。
- `font_files`：字体文件索引，包含格式、大小、修改时间、hash、状态、错误和预览支持状态。
- `font_faces`：字体 face 元数据，包含 family、style、full name、PostScript、weight、italic、glyph count、授权和版本信息。
- `favorites`：收藏 face。
- `install_records`：安装 / 卸载记录，包含源路径、目标路径、模式、范围、注册表 key / value 和状态。
- `operation_logs`：安装 / 卸载等操作日志。
- `scan_jobs`：后台扫描任务状态、进度和结果。
- `settings`：预留设置表。

### 扫描与索引

- 添加字体库、重新扫描和扫描系统字库都会异步启动扫描任务。
- 扫描会跳过 `.git`、`node_modules`、`dist` 等目录。
- 已索引文件在大小和修改时间未变化时会被跳过，减少大库重复扫描成本。
- 单个字体文件解析有 12 秒超时保护；超时或解析失败会写入 `error` 状态，不阻塞整个扫描任务。
- 应用启动时会把历史遗留的 `running` 扫描任务标记为 `interrupted`，避免界面误显示旧任务仍在运行。

### 预览缓存

- 前端只为可见字体和当前选中字体排队生成预览，默认并发为 2。
- 预览请求根据字体 hash、face index 和样本文字 hash 生成缓存 key。
- 后端会生成可供浏览器加载的 OTF 预览子集，并记录 glyph、缺字、完整字节数、子集字节数和 fallback 原因。
- 前端维护运行时 LRU，样本文字变化时清理已加载的 `FontFace` 并重新排队。

### Windows 字体安装边界

- 安装模式包括 `copy` 和 `link`；默认安装范围为当前用户 `user`。
- 所有用户范围 `machine` 需要管理员权限，否则后端安全失败并返回明确错误。
- 当前用户安装写入 `HKCU\Software\Microsoft\Windows NT\CurrentVersion\Fonts`；所有用户安装写入 `HKLM` 对应位置。
- 安装后调用 `AddFontResourceEx` 并广播 `WM_FONTCHANGE`。
- 卸载时会拒绝卸载受保护的系统字体目录内字体；复制安装的用户字体可按参数删除目标文件。

### 前端状态与交互

- 主界面由左侧字体库 / 文件夹导航、中间字体结果区、右侧详情面板组成。
- 支持全部字体、收藏、已安装筛选；支持搜索、视图模式切换、网格列数切换、样本文字和字号调整。
- 文件夹展开状态保存到 `localStorage`，键名为 `ziio.fontManager.expandedFolders.v1`。
- 字体列表每页 20 条，滚动接近底部时自动加载下一页。
- 安装进度通过 `font-operation-progress` 事件推送到前端，并用模态进度条展示。

## 已完成能力摘要

- v0.1：完成 Wails 应用壳、SQLite 存储、字体目录扫描、字体元数据解析、React 主界面、搜索 / 筛选、收藏、详情、当前用户安装、快捷安装、卸载和操作日志。
- v0.2：针对大字体库优化体验，添加字体库后立即返回，扫描转为后台执行；字体列表改为分页加载；增加系统字库扫描和用户 / 系统字库分区。
- v0.2.1：将独立“文件夹”菜单合并到字体库导航下，选中字体库后直接展开其文件夹树。
- v0.2.2：文件夹树改为按层级展开，并把展开状态持久化到浏览器 `localStorage`。
- v0.2.3：增加单文件解析 12 秒超时、错误字体入库和旧 `running` 扫描任务重置。
- v0.2.4：移除手动“再加载 20 个”按钮，改为触底自动加载下一页。

## 关键决策

- 使用 Wails v2.12，而不是仍处于 Alpha 标记的 Wails v3。
- 使用纯 Go SQLite 驱动 `modernc.org/sqlite`，避免要求本机安装 C 编译器。
- 持久化数据使用 Ziio 命名空间，默认路径为 `%APPDATA%\Ziio\FontManager`。
- 预览和缓存数据使用 Ziio 命名空间，默认路径为 `%LOCALAPPDATA%\Ziio\FontManager\cache`。
- WOFF / WOFF2 默认作为扫描和预览格式，不作为 Windows 系统字体安装格式。
- Type1、FON、FNT、FOT 等旧格式按“管理优先”处理：识别、入库、可由 Windows 支持时安装 / 卸载；预览不可用时给出明确提示。
- 禁止自动卸载系统核心字体。允许卸载外部非系统字体，但必须经过用户确认，并且默认不删除源文件。

## Beta 路线图

### B1 稳定性

- 增加扫描取消 / 重试能力，让用户可以主动停止超大目录或异常目录扫描。
- 在详情面板和列表中更清楚地展示错误字体、受限字体、解析超时和缺失文件状态。
- 完善历史扫描任务恢复策略，明确 `interrupted`、`complete_with_errors` 和 `error` 的界面表现。
- 针对超大库继续优化扫描批处理、进度刷新频率和 SQLite 写入压力。

### B2 安装体验

- 设计并实现所有用户安装 / 卸载的 UAC helper 或等效提权链，避免要求用户手动以管理员身份启动应用。
- 梳理卸载安全策略：系统字体保护、复制安装文件删除策略、快捷安装源文件缺失时的提示。
- 增强批量安装 / 卸载审计：结果摘要、失败原因聚合、操作日志查看入口。
- 明确 WOFF / WOFF2、旧格式和不可安装格式的按钮状态与文案。

### B3 预览性能

- 增加预览缓存清理策略，避免 `%LOCALAPPDATA%` 下缓存无限增长。
- 暴露预览生成状态和失败原因，便于定位某些字体无法预览的问题。
- 继续优化预览队列、并发、LRU 和样本文字变化后的刷新体验。
- 为旧格式、坏字体、缺字样本文本补齐一致的降级说明。

### B4 UI / 交互

- 打磨空状态、加载状态、长路径、长 family 名称和超长样本文字的布局表现。
- 改善批量选择体验，补充全选当前页、取消选择和批量操作前确认。
- 优化筛选组合、搜索反馈、系统字库入口和扫描中结果逐步出现的提示。
- 检查较小窗口下三栏布局、详情面板和网格列数的响应式表现。

### B5 发布工程

- 同步应用版本号：`App.GetAppInfo()`、README、发布包和构建元数据保持一致。
- 明确源码、依赖和构建产物的目录边界，避免误把 `node_modules`、`dist`、`build/bin` 当作源码交付。
- 固化发布命令和发布检查清单，包括 `go test ./...`、`npm run build`、`wails build -webview2 embed` 和 Windows 11 冒烟测试。
- 规划安装包 / 便携包输出、签名、升级策略和用户数据兼容说明。

## 测试与验证计划

- 后端：运行 `go test ./...`，覆盖格式识别、元数据解析、预览子集、SQLite 查询、收藏、安装记录、扫描任务和权限边界。
- 前端：运行 `npm run build`，确保 TypeScript 和 Vite 构建通过。
- 桌面构建：运行 `wails build -webview2 embed`，确认可生成便携版 EXE。
- 集成验证：在 Windows 11 上验证添加大字体库、扫描系统字库、搜索筛选、预览、安装、快捷安装、卸载和错误提示。
- 权限验证：分别以普通用户和管理员身份验证当前用户 / 所有用户安装路径。
- 回归验证：坏字体、慢字体、缺失文件、旧格式字体、超长路径和重复扫描不应拖住主流程。

## 验证日志

- 2026-06-24：后端服务、扫描器和存储层实现后，`go test ./...` 通过。
- 2026-06-24：React UI 实现后，`npm run build` 通过。
- 2026-06-24：补充字体格式解析、样本字体元数据解析、SQLite 查询 / 收藏 / 安装记录行为、注册表 value name 等单元测试后，`go test ./...` 再次通过。
- 2026-06-24：`wails build -webview2 embed` 通过，并生成便携版 EXE。
- 2026-06-24：便携版 EXE 启动冒烟测试通过：进程保持运行 5 秒，随后由测试脚本关闭。
- 2026-06-24：实现后台扫描、文件夹树、懒加载和系统字库分区后，`go test ./...` 通过。
- 2026-06-24：新增文件夹列表和文件夹筛选测试后，`go test ./...` 通过。
- 2026-06-24：实现 v0.2 前端改动后，`npm run build` 通过。
- 2026-06-24：重新执行 `wails build -webview2 embed` 通过，并更新当时的便携版 EXE。
- 2026-06-24：新版便携版 EXE 启动冒烟测试通过。
- 2026-06-24：合并字体库和文件夹导航后，`go test ./...` 与 `npm run build` 通过。
- 2026-06-24：实现文件夹树分层展开和展开状态持久化后，`go test ./...` 与 `npm run build` 通过。
- 2026-06-24：增加单文件解析超时和旧 running 任务重置后，`go test ./...` 与 `npm run build` 通过。
- 2026-06-24：触底自动加载实现后，修复 `App.tsx` 编码问题，`go test ./...` 与 `npm run build` 通过。

## Beta 风险

- Windows 字体安装行为受系统版本、注册表状态、权限和安全软件影响，需要在多环境中验证。
- 所有用户安装 / 卸载的自动提权链尚未实现，当前仍要求管理员身份运行应用。
- 部分旧字体格式和损坏字体无法提供可靠预览或完整元数据，需要保持优雅降级。
- 首次扫描超大字体库仍可能耗时较长，虽然不会阻塞添加操作，但扫描过程的可取消性和可解释性仍需增强。
- 当前已建立 Git 工作树并推送到 GitHub；后续仍需保持 `.gitignore`、生成产物和发布交付边界清晰。
- `frontend/node_modules`、`frontend/dist`、`build/bin` 已存在于项目目录中，后续需要持续避免将依赖或构建产物当作源码维护。
- 数据目录已切换为 Ziio 命名空间；早期本地测试数据不会自动迁移，需要重新扫描或手动导入。

## 当前文档更新说明

- 本文档已从 v0.2.4 实施记录整理为 Beta 开发计划。
- README 已更新为中英双语项目说明，覆盖当前技术栈、功能、目录结构、开发命令和 Beta 重点。
- 2026-07-03：品牌 / 应用名确定为 Ziio，完整名为 Ziio Font Manager；界面、窗口标题、HTML title、Wails 输出名和文档需同步使用新名称。
- 2026-07-03：本次品牌同步包含文档、前端界面、Wails 窗口标题、AppInfo 名称、构建输出文件名和前端生产构建产物；应用版本号仍暂不调整。
- 2026-07-03：应用数据和缓存目录正式切换为 Ziio 命名空间，README 移除历史数据位置说明。
